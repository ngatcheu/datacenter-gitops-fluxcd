---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: adminer
  namespace: adminer
spec:
  parentRefs:
    - name: traefik-gateway
      namespace: traefik
  hostnames:
    - adminer.homelab.local
  rules:
    - backendRefs:
        - name: adminer
          port: 80