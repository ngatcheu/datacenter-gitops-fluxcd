---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: vault-auth
  namespace: openldap
spec:
  vaultConnectionRef:
    name: vault-connection
    namespace: vault-secrets-operator-system
  method: jwt
  mount: clusterpayload
  jwt:
    role: jwt-clusterpayload-middleware-staging-openldap
    serviceAccount: openldap-vault-auth
    audiences:
      - middleware
---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: openldap-secret
  namespace: openldap
spec:
  vaultAuthRef: vault-auth
  mount: kv
  type: kv-v2
  path: middleware/staging/clusterpayload/openldap/openldap-secret
  destination:
    name: openldap-secret
    create: true
  refreshAfter: 30s
