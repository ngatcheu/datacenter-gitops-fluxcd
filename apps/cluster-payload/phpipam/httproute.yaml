---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: phpipam
  namespace: phpipam
spec:
  parentRefs:
    - name: traefik
      namespace: traefik
  hostnames:
    - phpipam.homelab.local
  rules:
    - backendRefs:
        - name: phpipam-web
          port: 80